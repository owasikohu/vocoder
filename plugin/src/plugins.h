#pragma once

#include <string>

// Set by clapEntryInit() to the plugin bundle path. Not currently used (the
// webview resources are served from memory), but kept for future file-based use.
extern std::string clapBundleResourceDir;
